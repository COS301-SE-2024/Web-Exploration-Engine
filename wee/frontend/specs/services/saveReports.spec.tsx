
describe('retrieveReports', () => {
  it('should access a users saved reports', () => {
    expect(true).toBe(true);
  })

  it('should return an error if the user is not logged in', () => {
    expect(true).toBe(true);
  })
});

describe('saveReports', () => {
  it('should save a users reports', () => {
    expect(true).toBe(true);
  })

  it('should return an error if the user is not logged in', () => {
    expect(true).toBe(true);
  })
});